// Require in all standard modules
const EventEmitter = require('events').EventEmitter;

class EventBlaster extends EventEmitter
{
    constructor()
    {
        
    }
}

module.exports = EventBlaster;